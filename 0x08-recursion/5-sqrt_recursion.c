#include "main.h"

/**
 * _sqrt_recursion - function that returns natural square root of a number
 * @n: the number to calculate dquare root of
 * Return: natural square root of a number
 */

int _sqrt_recursion(int n)
{
	if (n < 0)
	return (-1);
}
